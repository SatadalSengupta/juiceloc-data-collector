<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.satadal.juiceloc.DataCollectionActivity">

    <TextView
        android:id="@+id/toggleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginStart="50dp"
        android:layout_marginTop="17dp"
        android:text="@string/start_text"
        android:textStyle="bold" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/start_button_text"
        android:id="@+id/toggleButton"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="0dp"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="40dp" />

    <TextView
        android:id="@+id/sensorsTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/choose_sensors_text"
        android:textAlignment="center"
        android:textStyle="bold"
        android:layout_marginTop="15dp"
        android:layout_below="@+id/toggleButton"
        android:layout_marginStart="15dp"
        android:layout_marginEnd="15dp" />

    <ListView
        android:id="@+id/motionSensorsListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/sensorsTextView"
        android:layout_marginTop="12dp"
        android:layout_toStartOf="@+id/toggleButton"
        android:layout_alignParentEnd="true" />

    <ListView
        android:id="@+id/positionSensorsListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/motionSensorsListView"
        android:layout_marginTop="12dp"
        android:layout_toStartOf="@+id/toggleButton"
        android:layout_alignParentEnd="true" />

    <ListView
        android:id="@+id/environmentSensorsListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/positionSensorsListView"
        android:layout_marginTop="12dp"
        android:layout_toStartOf="@+id/toggleButton"
        android:layout_alignParentEnd="true" />

</RelativeLayout>
